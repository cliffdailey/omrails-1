$('#pins').append('<%= j render(@pins) %>').masonry( 'reload' );
<% if @pins.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@pins) %>')
<% else %>
	$('.pagination').remove();
<% end %>